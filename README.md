# Fabrique à histoires — Dockerized

Build and run with Docker:

```bash
docker build -t fabrique-histoires .
docker run -p 8080:80 fabrique-histoires
# then open http://localhost:8080
```

Local dev:

```bash
npm install
npm run dev
```
